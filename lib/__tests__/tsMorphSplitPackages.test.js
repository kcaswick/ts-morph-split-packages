const assert = require("assert");
const tsMorphSplitPackages = require("../index.js");

describe("tsMorphSplitPackages", () => {
  it("has a test", () => {
    assert(false, "tsMorphSplitPackages should have a test");
  });
});
